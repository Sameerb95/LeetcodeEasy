class Solution:
    def findNumbers(self, nums: List[int]) -> int:
        count = 0
        for i in map(str,nums):
            if len(i) %2 == 0:
                count +=1    
        return(count)
        


Runtime: 52 ms, faster than 75.30% of Python3 online submissions for Find Numbers with Even Number of Digits.
Memory Usage: 12.8 MB, less than 100.00% of Python3 online submissions for Find Numbers with Even Number of Digits.
