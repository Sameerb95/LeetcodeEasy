class Solution:
    def isPalindrome(self, x: int) -> bool:
        if(x < 0):
            return False
        s = str(x)
        b = s[::-1]
        b = int(b)
        if ( b == x):
            return True
        else:
            return False
            
            
            
Runtime: 56 ms, faster than 88.41% of Python3 online submissions for Palindrome Number.
Memory Usage: 12.8 MB, less than 100.00% of Python3 online submissions for Palindrome Number.
