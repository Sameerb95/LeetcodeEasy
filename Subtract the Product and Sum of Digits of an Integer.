class Solution:
    def subtractProductAndSum(self, n: int) -> int:
        add=0
        mul=1
        for i in str(n):
            add = int(add)+int(i)
            mul = int(mul)*int(i)
        
        return mul-add
        
Runtime: 28 ms, faster than 60.18% of Python3 online submissions for Subtract the Product and Sum of Digits of an Integer.
Memory Usage: 12.8 MB, less than 100.00% of Python3 online submissions for Subtract the Product and Sum of Digits of an Integer.
