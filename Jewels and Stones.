import re
class Solution:
    def numJewelsInStones(self, J: str, S: str) -> int:
        count = 0
        for i in J:
            count = count + len(re.findall(i,S))
        return(count)
        
Runtime: 24 ms, faster than 90.03% of Python3 online submissions for Jewels and Stones.
Memory Usage: 12.8 MB, less than 100.00% of Python3 online submissions for Jewels and Stones.
