from operator import ixor
import functools
class Solution:
    def xorOperation(self, n: int, start: int) -> int:   
        nums = []
        for i in range(n):
            nums.append(start + 2*i)

        return(functools.reduce(ixor,nums))
 
Runtime: 32 ms, faster than 67.73% of Python3 online submissions for XOR Operation in an Array.
Memory Usage: 13.9 MB, less than 100.00% of Python3 online submissions for XOR Operation in an Array.
